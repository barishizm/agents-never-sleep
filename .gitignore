# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Packaging build artifacts (pyproject.toml / pip wheel / python -m build)
build/
dist/
*.egg-info/
*.whl

# Runtime state — never commit a run's working files
.unattended/
**/.unattended/
state/
artifacts/
night-report*.md
morning-report*.md
acceptance/_runs/

# Local project config that may carry real IDs/paths
.claude/agents-never-sleep.json
**/capability-profile.json

# Editor / OS
.DS_Store
*.swp
night-report.md

# Download bundles — build artifacts, served from the site / shipped as release assets
*.tar.gz
*.zip
phase2-night-report*.md

# Internal working docs — host-only, NEVER distributed (parked design tracked in Paperclip)
CLAUDE.local.md
docs/superpowers/
COUNCIL-SETUP.md
BUILD-PLAN.md
PIP-INSTALL-PLAN.md
BUGFIX-PLAN-*.md

# Website — lives in the private repo TokonoMix/agentsneversleep-private, NEVER here.
# This repo is the OSS skill ONLY. Guard against the site ever re-entering the public tree.
/site/
/site-src/
/site-mockups/
/index.html

# Internal planning docs — removed from the public surface (security remediation); block re-adding.
docs/outreach/
docs/discoverability-notes.md
references/v1.1-aider-hermes-adapter-analysis.md
